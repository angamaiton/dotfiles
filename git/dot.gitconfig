# Global gitconfig

[alias]
  a       = add
  aa      = add .
  co      = checkout
  cb      = checkout -b

  rehard  = reset --hard
  st      = status
  s       = status --short --branch

# ============================================================================
# Local
# ============================================================================

[include]
  path = "~/.dotfiles/local/gitconfig"
